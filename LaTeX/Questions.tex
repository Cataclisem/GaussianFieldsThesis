{\Huge{\textbf{Questions from \today}}}

\subsection*{Question 1}
In the proof of Theorem 4.5 why does the second equality in the next equation hold?
\begin{equation}
	\e[\exp(i t X_s(f))] = \exp \left(-\frac{1}{2} t^2 \e [(X_s(f))^2] \right) = \exp\left(-\frac{1}{2}t^2\int_{K} f (-\Delta)^{-2s}f \dd \mu\right)
\end{equation}

\subsection*{Question 2}
In lemma 4.4 for $s = 0$ why is
\begin{equation}
	(-\Delta_m)^{0} f_m(p) = 1.
\end{equation}
I cannot get it to fit with the definition of $(-\Delta_m)^{-s}$

\subsection*{Question 3}
Again in the proof of lemma 4.4 you state that
\begin{equation}
	\sup_{m} \frac{1}{a_m} \sum_{p \in V_m} f_m(p)^2 < \infty.
\end{equation}
My guess is that it is because $V_m$ is a subset of the compact set $K$ and $f_m \in \ell(V_m) \subset C(K)$.

\subsection*{Question 4}
In the end of lemma 4.4, why does the following equality hold?
\begin{equation}
	\frac{1}{\Gamma(2s)} \int_{0}^{\infty} t^{2s-1} \int_{K}f(x)P_tf(x) \dd \mu(x) \dd t = \int_{K}f(x)(-\Delta)^{-2s}f(x) \dd \mu(x)
\end{equation}
